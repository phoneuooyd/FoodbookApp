<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
           xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
           xmlns:mct="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
           xmlns:components="clr-namespace:Foodbook.Views.Components"
           xmlns:loc="clr-namespace:FoodbookApp.Localization"
           x:Class="Foodbook.Views.Components.SearchablePickerPopup"
           x:Name="ThisPopup"
           CanBeDismissedByTappingOutsideOfPopup="True"
           Padding="0"
           Margin="0">

    <Border BackgroundColor="{DynamicResource PageBackgroundColor}"
            StrokeShape="RoundRectangle 12"
            StrokeThickness="1"
            Stroke="{DynamicResource Secondary}"
            MinimumWidthRequest="300"
            MaximumWidthRequest="500"
            MinimumHeightRequest="320"
            MaximumHeightRequest="600">
        <Grid RowDefinitions="Auto,*" Padding="0">
            <!-- Header (search bar + close button) - Grid instead of StackLayout -->
            <Grid Grid.Row="0"
                  BackgroundColor="{DynamicResource ShellBackgroundColor}"
                  Padding="12,10"
                  RowDefinitions="Auto"
                  ColumnDefinitions="*,Auto"
                  ColumnSpacing="0">
                <!-- Inner Grid for search bar + close button alignment -->
                <Grid Grid.Column="0"
                      Grid.ColumnSpan="2"
                      ColumnDefinitions="*,Auto"
                      Padding="6,6"
                      ColumnSpacing="6">
                    <!-- Modern search bar component -->
                    <components:ModernSearchBarComponent Grid.Column="0"
                                                         SearchText="{Binding SearchText, Source={x:Reference ThisPopup}}"
                                                         PlaceholderText="{x:Static loc:FolderResources.SearchPlaceholder}"
                                                         ClearCommand="{Binding ClearSearchCommand, Source={x:Reference ThisPopup}}" />

                    <!-- Close button -->
                    <Button Grid.Column="1"
                            Text="X"
                            FontSize="16"
                            WidthRequest="32"
                            HeightRequest="32"
                            CornerRadius="16"
                            BackgroundColor="Transparent"
                            TextColor="{DynamicResource ShellTitleColor}"
                            Command="{Binding CloseCommand, Source={x:Reference ThisPopup}}" />
                </Grid>
            </Grid>

            <!-- CollectionView replacing ScrollView+StackLayout -->
            <CollectionView Grid.Row="1"
                            x:Name="ItemsCollectionView"
                            Margin="8,4"
                            SelectionMode="None"
                            ItemsSource="{Binding FilteredItems, Source={x:Reference ThisPopup}}"
                            ItemSizingStrategy="MeasureFirstItem">
                <CollectionView.Header>
                    <Grid Padding="4,0,4,8"
                          IsVisible="{Binding ShowAddIngredientButton, Source={x:Reference ThisPopup}}">
                        <HorizontalStackLayout Spacing="12"
                                               VerticalOptions="Center">
                            <Button Text="+"
                                    WidthRequest="36"
                                    HeightRequest="36"
                                    CornerRadius="18"
                                    Padding="0"
                                    VerticalOptions="Center"
                                    StyleClass="Secondary"
                                    Command="{Binding AddIngredientCommand, Source={x:Reference ThisPopup}}" />
                            <Label Text="{x:Static loc:AddRecipePageResources.AddIngredient}"
                                   FontSize="15"
                                   VerticalOptions="Center"
                                   TextColor="{DynamicResource PrimaryText}">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding AddIngredientCommand, Source={x:Reference ThisPopup}}" />
                                </Label.GestureRecognizers>
                            </Label>
                        </HorizontalStackLayout>
                    </Grid>
                </CollectionView.Header>
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="6" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Button Text="{Binding .}"
                                HorizontalOptions="Fill"
                                BackgroundColor="Transparent"
                                BorderWidth="1"
                                CornerRadius="8"
                                Padding="12,10"
                                Margin="2,3"
                                FontSize="15"
                                TextColor="{DynamicResource PrimaryText}"
                                BorderColor="{DynamicResource Secondary}"
                                Command="{Binding SelectItemCommand, Source={x:Reference ThisPopup}}"
                                CommandParameter="{Binding .}" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Border>
</mct:Popup>