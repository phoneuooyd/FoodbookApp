<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Foodbook.Views.Components.SearchablePickerComponent"
             x:Name="ThisComponent">

    <!-- Simple picker-like interface that opens a search dialog -->
    <Border BackgroundColor="{AppThemeBinding Light=White, Dark=#2B2B2B}"
            Stroke="{AppThemeBinding Light=#E0E0E0, Dark=#404040}"
            StrokeThickness="1"
            StrokeShape="RoundRectangle 6"
            MinimumHeightRequest="44">
        
        <Grid ColumnDefinitions="*" Padding="12,8">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding OpenSelectionDialogCommand, Source={x:Reference ThisComponent}}" />
            </Grid.GestureRecognizers>

            <!-- Display text -->
            <Label Grid.Column="0"
                   FontSize="14"
                   VerticalOptions="Center"
                   LineBreakMode="TailTruncation"
                   Text="{Binding DisplayText, Source={x:Reference ThisComponent}}"
                   TextColor="{AppThemeBinding Light=Black, Dark=White}" />
        </Grid>
    </Border>
</ContentView>