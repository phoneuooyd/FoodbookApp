<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:loc="clr-namespace:Foodbook.Services"
             xmlns:vm="clr-namespace:Foodbook.ViewModels"
             xmlns:converters="clr-namespace:Foodbook.Converters"
             xmlns:resources="clr-namespace:FoodbookApp.Localization"
             xmlns:controls="clr-namespace:Foodbook.Views.Components"
             x:Class="Foodbook.Views.IngredientFormPage"
             x:Name="ThisPage"
             x:DataType="vm:IngredientFormViewModel"
             Title="{Binding Title}"
             Shell.NavBarIsVisible="False"
             HideSoftInputOnTapped="True"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    <ContentPage.Resources>
        <converters:StringToBoolConverter x:Key="StringToBoolConverter" />
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
        <converters:UnitToLocalizedStringConverter x:Key="UnitToStringConverter" />

        <Style x:Key="OverhaulCard" TargetType="Border">
            <Setter Property="BackgroundColor" Value="{DynamicResource OverhaulCardBackgroundColor}" />
            <Setter Property="Stroke" Value="{DynamicResource OverhaulCardStrokeColor}" />
            <Setter Property="StrokeThickness" Value="1.5" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="StrokeShape" Value="RoundRectangle 18" />
        </Style>

        <Style x:Key="SectionCaption" TargetType="Label">
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="CharacterSpacing" Value="1.3" />
            <Setter Property="TextColor" Value="{DynamicResource OverhaulSectionCaptionColor}" />
        </Style>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto">
        <Grid Grid.Row="0" Background="{DynamicResource AppHeroGradientBrush}" Padding="18,50,18,20">
            <Border WidthRequest="120" HeightRequest="120" HorizontalOptions="End" VerticalOptions="Start" TranslationX="20" TranslationY="-34" BackgroundColor="{DynamicResource AppHeroBubbleColorStrong}" StrokeThickness="0" StrokeShape="RoundRectangle 60" />
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" ZIndex="2">
                <Border WidthRequest="34" HeightRequest="34" BackgroundColor="{DynamicResource AppHeroActionOverlayColor}" StrokeThickness="0" StrokeShape="RoundRectangle 17" VerticalOptions="Start">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CancelCommand}" />
                    </Border.GestureRecognizers>
                    <Label Text="‹" FontSize="18" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" />
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="{Binding Title}" FontSize="26" FontAttributes="Bold" TextColor="White" />
                    <Label Text="{Binding Name}" FontSize="13" TextColor="{DynamicResource AppHeroSubtitleTextColor}" />
                </VerticalStackLayout>
            </Grid>
        </Grid>

        <controls:TabComponent x:Name="ContentHost" Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" SelectTabCommand="{Binding SelectTabCommand}" TabSize="Small">
            <controls:TabComponent.Tabs>
                <controls:TabItem Name="{loc:Translate Resource=IngredientFormPageResources, Key=BasicInfoTab}">
                    <controls:TabItem.Content>
                        <ScrollView>
                            <VerticalStackLayout Padding="16" Spacing="10">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=BasicInfoHeader}" Style="{StaticResource SectionCaption}" />

                                <Border Style="{StaticResource OverhaulCard}">
                                    <VerticalStackLayout Spacing="6">
                                        <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=NameLabel}" Style="{StaticResource FieldLabel}" />
                                        <Entry Placeholder="{loc:Translate Resource=IngredientFormPageResources, Key=NamePlaceholder}" Text="{Binding Name}" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" />
                                    </VerticalStackLayout>
                                </Border>

                                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                                    <Border Grid.Column="0" Style="{StaticResource OverhaulCard}">
                                        <VerticalStackLayout Spacing="6">
                                            <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=QuantityLabel}" Style="{StaticResource FieldLabel}" />
                                            <Entry Placeholder="100" Keyboard="Numeric" Text="{Binding Quantity}" HorizontalTextAlignment="Center" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" />
                                        </VerticalStackLayout>
                                    </Border>
                                    <Border Grid.Column="1" Style="{StaticResource OverhaulCard}">
                                        <VerticalStackLayout Spacing="6">
                                            <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=UnitLabel}" Style="{StaticResource FieldLabel}" />
                                            <controls:SimplePicker ItemsSource="{Binding Units}" SelectedItem="{Binding SelectedUnit}" ItemDisplayBinding="{Binding Converter={StaticResource UnitToStringConverter}}" />
                                        </VerticalStackLayout>
                                    </Border>
                                </Grid>

                                <Border Style="{StaticResource OverhaulCard}">
                                    <VerticalStackLayout Spacing="6">
                                        <Label Text="Waga 1 sztuki [g]" Style="{StaticResource FieldLabel}" />
                                        <Entry Placeholder="np. 60" Keyboard="Numeric" Text="{Binding UnitWeight}" HorizontalTextAlignment="Center" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" />
                                    </VerticalStackLayout>
                                </Border>

                                <Border IsVisible="{Binding IsPartOfRecipe}" BackgroundColor="{AppThemeBinding Light=#E6F4FF, Dark=#203040}" StrokeThickness="0" StrokeShape="RoundRectangle 10" Padding="10">
                                    <Label Text="{Binding RecipeInfo}" FontSize="12" HorizontalTextAlignment="Center" TextColor="{DynamicResource FrameTextColor}" />
                                </Border>
                            </VerticalStackLayout>
                        </ScrollView>
                    </controls:TabItem.Content>
                </controls:TabItem>

                <controls:TabItem Name="{loc:Translate Resource=IngredientFormPageResources, Key=NutritionTab}">
                    <controls:TabItem.Content>
                        <ScrollView>
                            <VerticalStackLayout Padding="16" Spacing="10">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=VerifyLabel}" Style="{StaticResource SectionCaption}" />

                                <Button Text="{loc:Translate Resource=IngredientFormPageResources, Key=VerifyButton}"
                                        Command="{Binding VerifyNutritionCommand}"
                                        IsEnabled="{Binding IsVerifying, Converter={StaticResource InvertedBoolConverter}}"
                                        HeightRequest="46"
                                        CornerRadius="14"
                                        BackgroundColor="{DynamicResource OverhaulAccentSoftColor}"
                                        BorderColor="{DynamicResource Primary}"
                                        BorderWidth="1.5"
                                        TextColor="{DynamicResource FrameTextColor}" />

                                <Border IsVisible="{Binding VerificationStatus, Converter={StaticResource StringToBoolConverter}}" Style="{StaticResource OverhaulCard}" BackgroundColor="{DynamicResource OverhaulInfoBannerBackgroundColor}" Stroke="{DynamicResource OverhaulInfoBannerStrokeColor}">
                                    <Label Text="{Binding VerificationStatus}" FontSize="12" HorizontalTextAlignment="Center" TextColor="{DynamicResource FrameTextColor}" />
                                </Border>

                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=NutritionHeader}" Style="{StaticResource SectionCaption}" />

                                <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource OverhaulCard}"><VerticalStackLayout Spacing="6"><Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=CaloriesLabel}" Style="{StaticResource FieldLabel}" /><Entry Placeholder="0.0" Keyboard="Numeric" Text="{Binding Calories}" HorizontalTextAlignment="Center" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" /></VerticalStackLayout></Border>
                                    <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource OverhaulCard}"><VerticalStackLayout Spacing="6"><Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=ProteinLabel}" Style="{StaticResource FieldLabel}" /><Entry Placeholder="0.0" Keyboard="Numeric" Text="{Binding Protein}" HorizontalTextAlignment="Center" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" /></VerticalStackLayout></Border>
                                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource OverhaulCard}"><VerticalStackLayout Spacing="6"><Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=FatLabel}" Style="{StaticResource FieldLabel}" /><Entry Placeholder="0.0" Keyboard="Numeric" Text="{Binding Fat}" HorizontalTextAlignment="Center" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" /></VerticalStackLayout></Border>
                                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource OverhaulCard}"><VerticalStackLayout Spacing="6"><Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=CarbsLabel}" Style="{StaticResource FieldLabel}" /><Entry Placeholder="0.0" Keyboard="Numeric" Text="{Binding Carbs}" HorizontalTextAlignment="Center" Focused="OnInputFocused" Unfocused="OnInputUnfocused" Style="{StaticResource ModernEntry}" /></VerticalStackLayout></Border>
                                </Grid>
                            </VerticalStackLayout>
                        </ScrollView>
                    </controls:TabItem.Content>
                </controls:TabItem>
            </controls:TabComponent.Tabs>
        </controls:TabComponent>

        <Grid Grid.Row="2" Padding="16,10,16,16" ColumnDefinitions="Auto,*" ColumnSpacing="10" BackgroundColor="{DynamicResource OverhaulFooterSurfaceColor}">
            <Button Grid.Column="0" Command="{Binding CancelCommand}" Text="{x:Static resources:ButtonResources.Cancel}" HeightRequest="48" CornerRadius="12" Padding="18,0" BackgroundColor="Transparent" BorderColor="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}" BorderWidth="1.5" TextColor="{DynamicResource FrameTextColor}" />
            <Button Grid.Column="1" Command="{Binding SaveCommand}" Text="{Binding SaveButtonText}" HeightRequest="48" CornerRadius="12" BackgroundColor="{DynamicResource Primary}" TextColor="{DynamicResource ButtonPrimaryText}" />
        </Grid>
    </Grid>
</ContentPage>
