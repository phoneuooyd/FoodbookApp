<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:loc="clr-namespace:Foodbook.Services"
             xmlns:vm="clr-namespace:Foodbook.ViewModels"
             xmlns:converters="clr-namespace:Foodbook.Converters"
             xmlns:resources="clr-namespace:FoodbookApp.Localization"
             x:Class="Foodbook.Views.IngredientFormPage"
             x:Name="ThisPage"
             x:DataType="vm:IngredientFormViewModel"
             Title="{Binding Title}">
    <ContentPage.Resources>
        <converters:StringToBoolConverter x:Key="StringToBoolConverter" />
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
    </ContentPage.Resources>
    
    <Grid RowDefinitions="Auto,*">
        
        <!-- Tab Bar -->
        <Grid Grid.Row="0" 
              ColumnDefinitions="*,*" 
              HeightRequest="{OnPlatform Default=60, iOS=70}"
              BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#2D2D30}">
            
            <!-- Tab 1: Informacje podstawowe -->
            <Button Grid.Column="0"
                    Text="{loc:Translate Resource=IngredientFormPageResources, Key=BasicInfoTab}"
                    Command="{Binding SelectTabCommand}"
                    CommandParameter="0"
                    BackgroundColor="{Binding IsBasicInfoTabSelected, Converter={StaticResource BoolToColorConverter}}"
                    TextColor="{Binding IsBasicInfoTabSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter='Text'}"
                    FontSize="12"
                    FontAttributes="{Binding IsBasicInfoTabSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter='Bold'}"
                    CornerRadius="0"
                    BorderWidth="0" />
            
            <!-- Tab 2: Wartości odżywcze -->
            <Button Grid.Column="1"
                    Text="{loc:Translate Resource=IngredientFormPageResources, Key=NutritionTab}"
                    Command="{Binding SelectTabCommand}"
                    CommandParameter="1"
                    BackgroundColor="{Binding IsNutritionTabSelected, Converter={StaticResource BoolToColorConverter}}"
                    TextColor="{Binding IsNutritionTabSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter='Text'}"
                    FontSize="12"
                    FontAttributes="{Binding IsNutritionTabSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter='Bold'}"
                    CornerRadius="0"
                    BorderWidth="0" />
        </Grid>
        
        <!-- Content Area -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="20" Spacing="15">

                <!-- Tab 1: Basic Information -->
                <StackLayout IsVisible="{Binding IsBasicInfoTabSelected}" Spacing="15">
                    
                    <!-- Header -->
                    <Label Text="{Binding Title}" 
                           FontSize="24" 
                           FontAttributes="Bold" 
                           HorizontalOptions="Center"
                           Margin="0,0,0,10" />
                    
                    <!-- Basic Information Section -->
                    <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=BasicInfoHeader}"
                           FontSize="18"
                           FontAttributes="Bold"
                           Margin="0,10,0,5" />
                    
                    <!-- Name Field -->
                    <Frame BorderColor="LightGray" 
                           Padding="8" 
                           CornerRadius="8">
                        <StackLayout Spacing="5">
                            <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=NameLabel}" 
                                   FontAttributes="Bold" 
                                   FontSize="14" />
                            <Entry Placeholder="{loc:Translate Resource=IngredientFormPageResources, Key=NamePlaceholder}"
                                   Text="{Binding Name}"
                                   ClearButtonVisibility="WhileEditing"
                                   FontSize="16" />
                        </StackLayout>
                    </Frame>
                    
                    <!-- Quantity and Unit in Grid -->
                    <Grid ColumnDefinitions="2*,*" ColumnSpacing="10">
                        <!-- Quantity Field -->
                        <Frame Grid.Column="0" 
                               BorderColor="LightGray" 
                               Padding="8" 
                               CornerRadius="8">
                            <StackLayout Spacing="5">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=QuantityLabel}" 
                                       FontAttributes="Bold" 
                                       FontSize="14" />
                                <Entry Placeholder="100" 
                                       Keyboard="Numeric" 
                                       Text="{Binding Quantity}"
                                       ClearButtonVisibility="WhileEditing"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16" />
                            </StackLayout>
                        </Frame>
                        
                        <!-- Unit Field -->
                        <Frame Grid.Column="1" 
                               BorderColor="LightGray" 
                               Padding="8" 
                               CornerRadius="8">
                            <StackLayout Spacing="5">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=UnitLabel}" 
                                       FontAttributes="Bold" 
                                       FontSize="14" />
                                <Picker Title="{loc:Translate Resource=IngredientFormPageResources, Key=UnitPickerTitle}"
                                        ItemsSource="{Binding Units}"
                                        SelectedItem="{Binding SelectedUnit}"
                                        FontSize="16" />
                            </StackLayout>
                        </Frame>
                    </Grid>
                    
                    <!-- Recipe Info (if applicable) -->
                    <StackLayout Spacing="5" IsVisible="{Binding IsPartOfRecipe}" Margin="0,15,0,0">
                        <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=InfoLabel}"
                               FontSize="14"
                               FontAttributes="Bold" />
                        <Frame BackgroundColor="LightBlue" 
                               Padding="10" 
                               CornerRadius="6">
                            <Label Text="{Binding RecipeInfo}" 
                                   FontSize="12"
                                   HorizontalTextAlignment="Center" />
                        </Frame>
                    </StackLayout>
                </StackLayout>

                <!-- Tab 2: Nutrition Values -->
                <StackLayout IsVisible="{Binding IsNutritionTabSelected}" Spacing="15">
                    
                    <!-- Nutritional Information Section -->
                    <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=NutritionHeader}"
                           FontSize="18"
                           FontAttributes="Bold"
                           Margin="0,10,0,5" />

                    <!-- OpenFoodFacts Verification Section -->
                    <StackLayout Spacing="8">
                        <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=VerifyLabel}"
                               FontSize="14"
                               FontAttributes="Bold"
                               Margin="0,0,0,8" />
                               
                        <Button Text="{loc:Translate Resource=IngredientFormPageResources, Key=VerifyButton}"
                                Command="{Binding VerifyNutritionCommand}"
                                BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                                TextColor="White"
                                FontSize="14"
                                FontAttributes="Bold"
                                HeightRequest="45"
                                CornerRadius="8"
                                Margin="0,0,0,8"
                                HorizontalOptions="FillAndExpand"
                                IsEnabled="{Binding IsVerifying, Converter={StaticResource InvertedBoolConverter}}" />
                        
                        <!-- Verification Status -->
                        <Label Text="{Binding VerificationStatus}" 
                               FontSize="12" 
                               HorizontalTextAlignment="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                               IsVisible="{Binding VerificationStatus, Converter={StaticResource StringToBoolConverter}}"
                               Margin="0,0,0,8" />
                    </StackLayout>
                    
                    <!-- Nutritional Values Grid -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" RowDefinitions="Auto,Auto" RowSpacing="10">
                        <!-- Calories Field -->
                        <Frame Grid.Row="0" Grid.Column="0" 
                               BorderColor="LightGray" 
                               Padding="8" 
                               CornerRadius="8">
                            <StackLayout Spacing="5">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=CaloriesLabel}" 
                                       FontAttributes="Bold" 
                                       FontSize="14" />
                                <Entry Placeholder="0.0" 
                                       Keyboard="Numeric" 
                                       Text="{Binding Calories}"
                                       ClearButtonVisibility="WhileEditing"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16" />
                            </StackLayout>
                        </Frame>
                        
                        <!-- Protein Field -->
                        <Frame Grid.Row="0" Grid.Column="1" 
                               BorderColor="LightGray" 
                               Padding="8" 
                               CornerRadius="8">
                            <StackLayout Spacing="5">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=ProteinLabel}" 
                                       FontAttributes="Bold" 
                                       FontSize="14" />
                                <Entry Placeholder="0.0" 
                                       Keyboard="Numeric" 
                                       Text="{Binding Protein}"
                                       ClearButtonVisibility="WhileEditing"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16" />
                            </StackLayout>
                        </Frame>
                        
                        <!-- Fat Field -->
                        <Frame Grid.Row="1" Grid.Column="0" 
                               BorderColor="LightGray" 
                               Padding="8" 
                               CornerRadius="8">
                            <StackLayout Spacing="5">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=FatLabel}" 
                                       FontAttributes="Bold" 
                                       FontSize="14" />
                                <Entry Placeholder="0.0" 
                                       Keyboard="Numeric" 
                                       Text="{Binding Fat}"
                                       ClearButtonVisibility="WhileEditing"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16" />
                            </StackLayout>
                        </Frame>
                        
                        <!-- Carbs Field -->
                        <Frame Grid.Row="1" Grid.Column="1" 
                               BorderColor="LightGray" 
                               Padding="8" 
                               CornerRadius="8">
                            <StackLayout Spacing="5">
                                <Label Text="{loc:Translate Resource=IngredientFormPageResources, Key=CarbsLabel}" 
                                       FontAttributes="Bold" 
                                       FontSize="14" />
                                <Entry Placeholder="0.0" 
                                       Keyboard="Numeric" 
                                       Text="{Binding Carbs}"
                                       ClearButtonVisibility="WhileEditing"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16" />
                            </StackLayout>
                        </Frame>
                    </Grid>
                </StackLayout>

                <!-- Action Buttons - Always visible at bottom -->
                <StackLayout Spacing="10" Margin="0,20,0,0">
                    <Button Text="{Binding SaveButtonText}"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                            TextColor="White"
                            HeightRequest="50"
                            FontSize="16"
                            FontAttributes="Bold"
                            CornerRadius="8"
                            HorizontalOptions="FillAndExpand" />
                </StackLayout>
                
                <!-- Validation Message -->
                <Label Text="{Binding ValidationMessage}" 
                       FontSize="12"
                       IsVisible="{Binding HasValidationError}"
                       HorizontalTextAlignment="Center"
                       TextColor="Red"
                       Margin="0,10,0,0" />
               
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
